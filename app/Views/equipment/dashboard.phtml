<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Equipment Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container my-5">

    <!-- PAGE TITLE -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold">Tableau de bord des emprunts</h2>
        <a href="index.php?controller=equipment&action=create" class="btn btn-primary">
            Nouvel emprunt
        </a>
    </div>

    <!-- SUMMARY CARDS -->
    <div class="row g-3 mb-5">
        <div class="col-md-2">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Total</h6>
                    <h4 class="fw-bold"><?= $stats['Total'] ?? 0 ?></h4>
                </div>
            </div>
        </div>
        <div class="col-md-2">
            <div class="card shadow-sm border-success border-start border-4">
                <div class="card-body text-center">
                    <h6 class="text-success mb-1">Disponible</h6>
                    <h4 class="fw-bold"><?= $stats['Disponible'] ?? 0 ?></h4>
                </div>
            </div>
        </div>
        <div class="col-md-2">
            <div class="card shadow-sm border-warning border-start border-4">
                <div class="card-body text-center">
                    <h6 class="text-warning mb-1">Prêté(e)</h6>
                    <h4 class="fw-bold"><?= $stats['Prete'] ?? 0 ?></h4>
                </div>
            </div>
        </div>
        <div class="col-md-2">
            <div class="card shadow-sm border-info border-start border-4">
                <div class="card-body text-center">
                    <h6 class="text-info mb-1">En maintenance</h6>
                    <h4 class="fw-bold"><?= $stats['En maintenance'] ?? 0 ?></h4>
                </div>
            </div>
        </div>
        <div class="col-md-2">
            <div class="card shadow-sm border-danger border-start border-4">
                <div class="card-body text-center">
                    <h6 class="text-danger mb-1">Hors service</h6>
                    <h4 class="fw-bold"><?= $stats['Hors service'] ?? 0 ?></h4>
                </div>
            </div>
        </div>
    </div>

    <!-- EQUIPMENT TABLE -->
    <div class="card shadow-sm mb-5">
        <div class="card-header bg-white">
            <h5 class="fw-bold mb-0">Liste du matériel</h5>
        </div>
        <div class="card-body">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Nom</th>
                        <th>Catégorie</th>
                        <th>Numéro série</th>
                        <th>Etat</th>
                        <th>Prêt en cours</th>
                        <th>Acheté le</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (!empty($equipments)): ?>
                        <?php foreach ($equipments as $eq): ?>
                            <tr>
                                <td><?= htmlspecialchars($eq->name) ?></td>
                                <td><?= htmlspecialchars($eq->category ?? '-') ?></td>
                                <td><?= htmlspecialchars($eq->serial_number ?? '-') ?></td>
                                <td>
                                    <?php
                                    $colors = [
                                        'Disponible' => 'success',
                                        'Prete' => 'warning',
                                        'En maintenance' => 'info',
                                        'Hors service' => 'danger'
                                    ];
                                    $badge = $colors[$eq->state] ?? 'secondary';
                                    ?>
                                    <span class="badge bg-<?= $badge ?>">
                                        <?= ucfirst($eq->state) ?>
                                    </span>
                                </td>
                                <td><?= $eq->active_loans ?? 0 ?></td>
                                <td><?= htmlspecialchars($eq->purchase_date ?? '-') ?></td>
                                <td>
                                    <a href="index.php?controller=equipment&action=show&id=<?= $eq->id ?>" class="btn btn-sm btn-outline-secondary">Consulter</a>
                                    <a href="index.php?controller=equipment&action=edit&id=<?= $eq->id ?>" class="btn btn-sm btn-outline-primary">Modifier</a>
                                    <a href="index.php?controller=equipment&action=delete&id=<?= $eq->id ?>" class="btn btn-sm btn-outline-danger">Supprimer</a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr><td colspan="8" class="text-center text-muted">Aucun matériel trouvé.</td></tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>

    <!-- ACTIVE LOANS TABLE -->
    <div class="card shadow-sm">
        <div class="card-header bg-white">
            <h5 class="fw-bold mb-0">Prêts en cours</h5>
        </div>
        <div class="card-body">
            <table class="table table-striped align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Matériel</th>
                        <th>Bénéficiaire</th>
                        <th>Type de bénéficiaire</th>
                        <th>Emprunté(e) le</th>
                        <th>Retourné(e) le</th>
                        <th>Etat</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (!empty($activeLoans)): ?>
                        <?php foreach ($activeLoans as $loan): ?>
                            <tr>
                                <td><?= htmlspecialchars($loan->equipment_name) ?></td>
                                <td><?= htmlspecialchars($loan->grantee) ?></td>
                                <td><?= ucfirst($loan->grantee_type) ?></td>
                                <td><?= htmlspecialchars($loan->loaned_at) ?></td>
                                <td><?= htmlspecialchars($loan->return_at ?? '-') ?></td>
                                <td>
                                    <?php
                                    $now = new DateTime();
                                    $due = new DateTime($loan->return_at);
                                    $isLate = $loan->return_at && $now > $due;
                                    ?>
                                    <span class="badge bg-<?= $isLate ? 'danger' : 'success' ?>">
                                        <?= $isLate ? 'En retard' : 'A l\'heure' ?>
                                    </span>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr><td colspan="6" class="text-center text-muted">Aucun prêt en cours.</td></tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
